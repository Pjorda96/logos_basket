{% extends 'base.html.twig' %}
{% block body %}
    {#{% if error %}
            <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        {% endif %}
    
        <form action="{{ path('login') }}" method="post">
            <label for="username">Username:</label>
            <input type="text" id="username" name="_username" value="{{ last_username }}" />
    
            <label for="password">Password:</label>
            <input type="password" id="password" name="_password" />
    
            <button type="submit">login</button>
            <a href="{{ path('user_registration') }}">Registro</a>
        </form>#}
    <div class="container">
        <div class="row">
            <div
                class="col-sm-9 col-md-7 col-lg-5 mx-auto" style="margin-top:6rem">
                {# <div class="card card-signin my-5" style="background-color:background-color: rgba(223, 153, 48, 0.877);
                    border-radius: 100px 10px 10px 10px;"> #}
                <div class="card-body">
                    <div class="row">
                        <img class="mx-auto logo" src="{{ asset('img/imagenlogo.png') }}"></div>
                        <h5 class="card-title text-center carta">Entrar</h5>
                        {% if error %}
                            <div>
                                {{ error.messageKey|trans(error.messageData, 'security') }}</div>
                        {% endif %}
                        <div class="container">
                            <div class="row">
                                <div class="col-md-6 mx-auto" >
                                    <label for="emailOrUser" class="select">Escoja uno:</label>
                                    {# SIRVE PARA DAR FORMATO AL SELECT #}
                                    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" rel="stylesheet">
                                        <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-select/1.13.1/css/bootstrap-select.css" rel="stylesheet"/>
                                        <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>
                                        <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/js/bootstrap.bundle.min.js"></script>
                                        <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-select/1.13.1/js/bootstrap-select.min.js"></script>
                                        {# SIRVE PARA DAR FORMATO AL SELECT #}
                                        <div class="dropdown">
                                            <select class="form-control" id="select">
                                                <option selected>Choose one...</option>
                                                <option>DNI</option>
                                                <option>PASAPORTE</option>
                                                <option>NIE</option>
                                            </select>
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <label for="emailOrUser">Usuario:</label>
                                        <input type="text" id="emailOrUser" class="form-control" name="_username" value="{{ last_username }}" placeholder="Usuario" required autofocus></div>
                                    </div>
                                    <div class="form-label-group pass">
                                        <label for="inputPassword">Contraseña:</label>
                                        <input class="form-control" id="inputPassword" name="_password" placeholder="Contraseña" required type="password"></div>
                                        <br>
                                            <button class="btn btn-lg btn-primary btn-block text-uppercase" type="submit">Entrar
                                            </button>
                                        </form>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                {% endblock %}
                {% block stylesheets %}
                    <link rel="stylesheet" href="{{ asset('css/login.css') }}">
                    {% endblock %}
                