{% extends 'base.html.twig' %}
{% block body %}
    {#<div class="form-signin" onsubmit="return validform()">
        {{ form_start(form) }}
        <div>
            {{ form_label(form.email) }}<br>
            {{ form_widget(form.email, {'attr': {'required': 'required'}}) }}<br>
            {{ form_errors(form.email) }}
        </div>
        <div>
            {{ form_widget(form.plainPassword, {'attr': {'required': 'required'}}) }}<br>
            {{ form_errors(form.plainPassword) }}
        </div>
        <div>
            {{ form_label(form.nombre) }}<br>
            {{ form_widget(form.nombre, {'attr': {'required': 'required'}}) }}<br>
            {{ form_errors(form.nombre) }}
        </div>
        <div>
            {{ form_label(form.apellido_1) }}<br>
            {{ form_widget(form.apellido_1, {'attr': {'required': 'required'}}) }}<br>
            {{ form_errors(form.apellido_1) }}
        </div>
        <div>
            {{ form_label(form.apellido_2) }}<br>
            {{ form_widget(form.apellido_2, {'attr': {'required': 'required'}}) }}<br>
            {{ form_errors(form.apellido_2) }}
        </div>
        <div>
            {{ form_label(form.usuario) }}<br>
            {{ form_widget(form.usuario, {'attr': {'required': 'required'}}) }}<br>
            {{ form_errors(form.usuario) }}
        </div>
        <div>
            {{ form_label(form.telefono) }}<br>
            {{ form_widget(form.telefono) }}<br>
            {{ form_errors(form.telefono) }}
        </div>
        <br>
        <br>
        <button class="btn btn-lg btn-primary btn-block text-uppercase" type="submit">Entrar
        </button>
        <div class="custom-control mb-3" style="padding-top: 1rem; padding-left: 0">
            Ya tengo cuenta. <a href="{{ path('login') }}">Entrar</a>
        </div>
        <hr class="my-4">
        <button class="btn btn-lg btn-google btn-block text-uppercase" type="button"><i
                    class="fab fa-google mr-2"></i> Entrar con Google
        </button>
        <button class="btn btn-lg btn-facebook btn-block text-uppercase" type="button"><i
                    class="fab fa-facebook-f mr-2"></i> Entrar con Facebook
        </button>
        {{ form_end(form) }}
    </div>#}
    <div class="container">
        <div class="row">
            <div class="col-sm-9 col-md-7 col-lg-5 mx-auto" style="margin-top:6rem">
                {# <div class="card card-signin my-5" style="background-color:background-color: rgba(223, 153, 48, 0.877);
                    border-radius: 100px 10px 10px 10px;"> #}
                <div class="card-body">
                    <div class="row">
                        <img class="mx-auto logo" src="{{ asset('img/imagenlogo.png') }}"></div>
                    <h5 class="card-title text-center carta">Entrar</h5>
                    <div class="container">
                        {{ form_start(form) }}
                        <div class="row">
                            <div class="col-md-6 mx-auto">
                                <label class="select">Escoja uno:</label>
                                <div class="dropdown">
                                    <select class="form-control" id="select">
                                        <option selected>DNI</option>
                                        <option>PASAPORTE</option>
                                        <option>NIE</option>
                                    </select>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <label for="emailOrUser">Nº Documento:</label>
                                <input type="text" id="emailOrUser" class="form-control" name="_username"
                                       placeholder="Nº Documento" required autofocus></div>
                        </div>
                        <div class="form-label-group pass">
                            <label for="inputPassword">Contraseña:</label>
                            <input class="form-control" id="inputPassword" name="_password" placeholder="Contraseña"
                                   required type="password"></div>
                        <div class="form-label-group pass mt-3">
                            <label for="inputPassword">Repetir Contraseña:</label>
                            <input class="form-control" id="inputPassword" name="_password"
                                   placeholder=" Repetir Contraseña" required type="password"></div>
                        <p class="mt-4">¿Ya tienes cuenta? <a href="{{ path("login") }}">Login</a></p>
                        <button class="btn btn-lg btn-primary btn-block text-uppercase" type="submit">Registrarse
                        </button>
                        {{ form_end(form) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/registro.css') }}">
{% endblock %}

{% block javascripts %}
    {# SIRVE PARA DAR FORMATO AL SELECT #}
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-select/1.13.1/css/bootstrap-select.css"
          rel="stylesheet"/>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-select/1.13.1/js/bootstrap-select.min.js"></script>
    {# SIRVE PARA DAR FORMATO AL SELECT #}
{% endblock %}
                
